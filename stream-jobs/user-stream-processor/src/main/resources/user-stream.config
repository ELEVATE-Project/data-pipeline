include "base-config.conf"

kafka {
  input.topic = "dev.userCreate"
  groupId = "dev.users"
  output.topic = "sl-metabase-user-dashboard-dev"
}

task {
  consumer.parallelism = 1
  sl.users.stream.parallelism = 1
  sl.metabase.dashboard.parallelism = 1
}

postgres{
  host = "localhost"
  port = "5432"
  username = "postgres"
  password = "Test@123"
  database = "test"
  tables = {
%     usersTable = ${job.env}"_users"
      shikshagrahaTable = ${job.env}"_shikshagraha"
      shikshalokamTable = ${job.env}"_shikshalokam"
  }
}
