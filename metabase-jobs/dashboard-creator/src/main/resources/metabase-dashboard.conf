include "base-config.conf"

kafka {
  input.topic = "sl-metabase-dashboard-dev"
  groupId = "sl-metabase-dashboard-consumer-group-dev"
}

task {
  sl.metabase.dashboard.parallelism = 1
}

postgres{
  host = "localhost"
  port = "5432"
  username = "postgres"
  password = "elevatedata"
  database = "dev-project-analytics"
  tables = {
    adminMetadata = "admin_dashboard_metadata"
    stateMetadata = "state_dashboard_metadata"
    programMetadata = "program_dashboard_metadata"
    districtMetadata = "district_dashboard_metadata"}
}

metabase {
  url = "http://localhost:3000/api"
  username = "metabase-username"
  password = "metabase-password"
  database = "ElevateData"
}

filePath {
    ProjectJsonDir = "/home/user1/Documents/elevate2/elevate-data-pipeline/metabase-jobs/dashboard-creator/src/main/scala/org/shikshalokam/job/dashboard/creator/utils/projectJson"
}