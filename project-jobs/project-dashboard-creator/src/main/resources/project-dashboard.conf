include "base-config.conf"

kafka {
  input.topic = "sl-project-metabase-dashboard-dev"
  groupId = "sl-project-metabase-dashboard-consumer-group-dev"
}

task {
  sl.projects.dashboard.parallelism = 1
}

postgres{
  host = "localhost"
  port = "5432"
  username = "postgres"
  password = "elevatedata"
  database = "dev-project-analytics"
}